# Services default parameter - equivalent to service-parameters.yml.
services:
  - name: api
    parameters:
      - name: HOST_PORT
        value: 2077
      - name: DB_DATABASE
        value: "api_test"

  - name: db
    parameters:
      - name: DB_PORT
        value: 2099
      - name: POSTGRES_PASSWORD
        value: "kgalli"
      - name: POSTGRES_USER
        value: "kgalli"
